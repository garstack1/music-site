{
  "installCommand": "npm install",
  "buildCommand": "cat prisma/schema.prisma | head -170 | tail -20 && rm -rf node_modules/.prisma && npx prisma generate --schema=./prisma/schema.prisma && npm run build"
}
